{
  "mcpServers": {
    "laravel-boost": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "zenonhub-app-1",
        "php",
        "/var/www/html/artisan",
        "boost:mcp"
      ]
    }
  }
}
