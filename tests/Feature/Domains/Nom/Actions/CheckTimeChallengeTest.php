<?php

declare(strict_types=1);

uses()->group('nom', 'nom-actions', 'check-time-challenge');

it('creates a time challenge if one doesnt exist', function () {

});

it('returns an existing challenge if one already exists', function () {

});
