<?php

declare(strict_types=1);

uses()->group('site', 'routes', 'terms');

//it('terms and conditions page returns a successful response', function () {
//    $response = $this->get(route('terms'));
//    $response->assertStatus(200);
//});
